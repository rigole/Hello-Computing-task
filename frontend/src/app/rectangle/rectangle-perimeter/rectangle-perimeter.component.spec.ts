/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { RectanglePerimeterComponent } from './rectangle-perimeter.component';

describe('RectanglePerimeterComponent', () => {
  let component: RectanglePerimeterComponent;
  let fixture: ComponentFixture<RectanglePerimeterComponent>;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [RectanglePerimeterComponent]
    });
    fixture = TestBed.createComponent(RectanglePerimeterComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/